---
import Layout from '@/layouts/Layout.astro'
import { Button } from '@/components/ui/button'
import { Card, CardContent, CardDescription, CardFooter, CardHeader, CardTitle } from '@/components/ui/card'
import { CheckCircle } from 'lucide-react'
---

<Layout
  title="Confirmación - Dominican HCI Club"
  description="Tu cuenta ha sido creada exitosamente"
>
  <div class="min-h-screen bg-background flex items-center justify-center p-4">
    <div class="w-full max-w-md">
      <Card client:load>
        <CardHeader client:load className="text-center">
          <div class="flex justify-center mb-4">
            <CheckCircle client:load className="w-16 h-16 text-[rgba(25,42,110,1)]" />
          </div>
          <CardTitle client:load className="text-2xl text-[rgba(25,42,110,1)]">¡Bienvenid@ al Dominican HCI Club!</CardTitle>
          <CardDescription client:load>Tu cuenta ha sido creada exitosamente</CardDescription>
        </CardHeader>
        <CardContent client:load className="text-center space-y-4">
          <p class="text-muted-foreground">
            Estamos emocionados de tenerte en nuestro espacio. Comienza a explorar proyectos, conecta con otros profesionales y transforma tu aprendizaje    
          </p>
        </CardContent>
        <CardFooter client:load className="flex flex-col gap-3">
          <Button client:load asChild className="w-full bg-[rgba(25,42,110,1)]">
            <a href="/welcome">Explorar contenido</a>
          </Button>
          <Button client:load asChild variant="outline" className="w-full bg-transparent text-[rgba(25,42,110,1)]">
            <a href="/profile">Ir a mi perfil</a>
          </Button>
        </CardFooter>
      </Card>
    </div>
  </div>
</Layout>
